# Copy this file to .env once the FastAPI backend (Section 3.1.2 of the
# proposal) is deployed on Render/Railway, then set USE_MOCK = false in
# src/api/potholeApi.js to switch from dummy data to the live API.

VITE_API_BASE_URL=https://your-fastapi-app.onrender.com
